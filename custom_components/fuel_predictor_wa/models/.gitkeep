# Trained model artifacts live here (generated by tools/train.py).
# Bundled into the integration so the forecaster works without retraining on the HA host.
# Artifacts (*.pkl / *.npz / *.joblib) are gitignored; this file keeps the dir.
